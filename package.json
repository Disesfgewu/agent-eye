{
  "name": "agent-eye",
  "version": "0.1.0",
  "private": true,
  "description": "Give AI coding agents eyes and hands in the browser: an MCP server + VS Code extension for autonomous frontend run-test-debug loops.",
  "license": "MIT",
  "workspaces": [
    "packages/mcp-server",
    "packages/vscode-extension"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
